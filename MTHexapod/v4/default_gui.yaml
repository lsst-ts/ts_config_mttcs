---

# Connection timeout in second.
connection_timeout: 10

# Configuration of the camera hexapod PXI controller.
camera_config:
  # Host name.
  host: "camhex-pxi-controller.cp.lsst.org"
  # Port number.
  port: 5560

# Configuration of the M2 hexapod PXI controller.
m2_config:
  # Host name.
  host: "m2-hexapod-pxi.cp.lsst.org"
  # Port number.
  port: 5550
